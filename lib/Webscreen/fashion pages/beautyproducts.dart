import 'package:flutter/material.dart';




class BeautyProducts extends StatelessWidget {
  const BeautyProducts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}