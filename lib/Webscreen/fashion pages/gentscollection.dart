import 'package:flutter/material.dart';










class GentsCollection extends StatelessWidget {
  const GentsCollection({super.key});

  @override
  Widget build(BuildContext context) {
    return const  Scaffold(
      body: Text("Gents"),
    );
  }
}